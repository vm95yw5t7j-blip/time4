# Repository Working Rules

- If a copy interaction is added, tapping the copy target once must copy it. After a successful copy, show visible confirmation that the copy completed.
- Do not deploy or publish this repository to Netlify, or change its Netlify configuration, unless the user explicitly requests it.

